---
- name: Gather machineset details from {{ target_cluster.cluster_name }}
  shell: |
    export KUBECONFIG={{ target_cluster.install_dir }}/auth/kubeconfig
    oc get machineset.machine.openshift.io -n openshift-machine-api -o json | jq '.items[0]'
  register: base_machineset
  changed_when: false

- name: Parse base machineset for {{ target_cluster.cluster_name }}
  set_fact:
    ms_json: "{{ base_machineset.stdout | from_json }}"

- name: Extract cluster infrastructure ID for {{ target_cluster.cluster_name }}
  set_fact:
    cluster_infra_id: "{{ ms_json.metadata.labels['machine.openshift.io/cluster-api-cluster'] }}"
    managed_cluster_name: "{{ 'local-cluster' if target_cluster.cluster_name == 'hub' else target_cluster.cluster_name }}"

- name: Create machineset template from existing one for {{ target_cluster.cluster_name }}
  shell: |
    export KUBECONFIG={{ target_cluster.install_dir}}/auth/kubeconfig
    oc get machineset.machine.openshift.io -n openshift-machine-api -o yaml | head -1 | \
    jq '{
      "apiVersion": "machine.openshift.io/v1beta1",
      "kind": "MachineSet",
      "metadata": {
        "name": "{{ cluster_infra_id }}-{{ bare_metal_machineset_name }}",
        "namespace": "openshift-machine-api",
        "labels": .items[0].metadata.labels
      },
      "spec": {
        "replicas": {{ bare_metal_replicas }},
        "selector": {
          "matchLabels": {
            "machine.openshift.io/cluster-api-cluster": "{{ cluster_infra_id }}",
            "machine.openshift.io/cluster-api-machineset": "{{ cluster_infra_id }}-{{ bare_metal_machineset_name }}"
          }
        },
        "template": {
          "metadata": {
            "labels": {
              "machine.openshift.io/cluster-api-cluster": "{{ cluster_infra_id }}",
              "machine.openshift.io/cluster-api-machine-role": "worker",
              "machine.openshift.io/cluster-api-machine-type": "worker",
              "machine.openshift.io/cluster-api-machineset": "{{ cluster_infra_id }}-{{ bare_metal_machineset_name }}",
              "node-role.kubernetes.io/worker-cnv": ""
            }
          },
          "spec": (.items[0].spec.template.spec | {
            "metadata": {
              "labels": {
                "node-role.kubernetes.io/worker-cnv": ""
              }
            },
            "providerSpec": {
              "value": (.providerSpec.value | .instanceType = "{{ bare_metal_instance_type }}")
            }
          })
        }
      }
    }'
  register: machineset_template
  changed_when: false

- name: Create ACM Policy for {{ target_cluster.cluster_name }} via shell
  shell: |
    export KUBECONFIG={{ hub_kubeconfig }}
    cat <<'EOF' | oc apply -f -
    apiVersion: policy.open-cluster-management.io/v1
    kind: Policy
    metadata:
      name: bare-metal-machineset-{{ target_cluster.cluster_name }}
      namespace: bare-metal-policies
    spec:
      disabled: false
      remediationAction: enforce
      policy-templates:
      - objectDefinition:
          apiVersion: policy.open-cluster-management.io/v1
          kind: ConfigurationPolicy
          metadata:
            name: bare-metal-machineset-{{ target_cluster.cluster_name }}-config
          spec:
            remediationAction: enforce
            severity: high
            object-templates:
            - complianceType: musthave
              objectDefinition: {{ machineset_template.stdout }}
    EOF
  environment: "{{ hub_kubeconfig_env }}"
  changed_when: true

- name: Create Placement for {{ target_cluster.cluster_name }} via shell
  shell: |
    export KUBECONFIG={{ hub_kubeconfig }}
    cat <<'EOF' | oc apply -f -
    apiVersion: cluster.open-cluster-management.io/v1beta1
    kind: Placement
    metadata:
      name: placement-{{ target_cluster.cluster_name }}
      namespace: bare-metal-policies
    spec:
      predicates:
      - requiredClusterSelector:
          labelSelector:
            matchLabels:
              name: {{ managed_cluster_name }}
    EOF
  environment: "{{ hub_kubeconfig_env }}"
  changed_when: true

- name: Create PlacementBinding for {{ target_cluster.cluster_name }} via shell
  shell: |
    export KUBECONFIG={{ hub_kubeconfig }}
    cat <<'EOF' | oc apply -f -
    apiVersion: policy.open-cluster-management.io/v1
    kind: PlacementBinding
    metadata:
      name: bare-metal-machineset-binding-{{ target_cluster.cluster_name }}
      namespace: bare-metal-policies
    spec:
      placementRef:
        apiGroup: cluster.open-cluster-management.io
        kind: Placement
        name: placement-{{ target_cluster.cluster_name }}
      subjects:
      - apiGroup: policy.open-cluster-management.io
        kind: Policy
        name: bare-metal-machineset-{{ target_cluster.cluster_name }}
    EOF
  environment: "{{ hub_kubeconfig_env }}"
  changed_when: true

- name: Display policy created for {{ target_cluster.cluster_name }}
  debug:
    msg: "Created ACM policy for {{ target_cluster.cluster_name }} bare metal machineset ({{ cluster_infra_id }}-{{ bare_metal_machineset_name }})"
