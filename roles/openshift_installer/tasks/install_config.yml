---
- name: Check if install-config.yaml already exists
  stat:
    path: "{{ install_dir }}/install-config.yaml"
  register: install_config_stat

- name: Check if cluster already exists
  stat:
    path: "{{ install_dir }}/auth/kubeconfig"
  register: kubeconfig_stat

- name: Read pull secret from bastion
  slurp:
    src: /home/ec2-user/pull-secret.json
  register: pull_secret_bastion
  when:
    - not install_config_stat.stat.exists
    - not kubeconfig_stat.stat.exists

- name: Read SSH public key from bastion
  slurp:
    src: /home/ec2-user/.ssh/openshift-key.pub
  register: ssh_public_key_bastion
  when:
    - not install_config_stat.stat.exists
    - not kubeconfig_stat.stat.exists

- name: Set decoded file contents
  set_fact:
    pull_secret_content: "{{ pull_secret_bastion.content | b64decode | from_json | to_json }}"
    ssh_public_key_content: "{{ ssh_public_key_bastion.content | b64decode | trim }}"
  when:
    - not install_config_stat.stat.exists
    - not kubeconfig_stat.stat.exists

- name: Create install directory
  file:
    path: "{{ install_dir }}"
    state: directory
    mode: '0755'
  when:
    - not install_config_stat.stat.exists
    - not kubeconfig_stat.stat.exists

- name: Create install-config.yaml from template
  template:
    src: install-config.yaml.j2
    dest: "{{ install_dir }}/install-config.yaml"
    mode: '0644'
  when:
    - not install_config_stat.stat.exists
    - not kubeconfig_stat.stat.exists

- name: Backup install-config.yaml
  copy:
    src: "{{ install_dir }}/install-config.yaml"
    dest: "{{ install_dir }}/install-config.yaml.backup"
    remote_src: yes
    mode: '0644'
  when: install_config_stat.stat.exists or (not kubeconfig_stat.stat.exists)
